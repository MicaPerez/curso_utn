import { getAdapter } from 'axios';
import React from 'react';

const Header = (props) => {
    return(
        <header>
            <div className="holder">
                <h1>Estetica Micaela</h1>
            </div>
        </header>
    );
}

export default Header;